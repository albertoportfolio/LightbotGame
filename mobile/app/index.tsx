import WebViewGame from '../src/WebViewGame';

// Pantalla principal de la app: renderiza el juego web dentro de un WebView a pantalla completa
export default function HomeScreen() {
  return <WebViewGame />;
}
